import React from 'react';
import Home from './Home';

// Since index.php is basically the same as home.php from previous implementation, 
// we will just export Home to serve as Index.
const Index = () => {
    return <Home />;
};

export default Index;
